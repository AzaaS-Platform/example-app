{{>header}}

<h1>{{title}}</h1>

<table class="table">
    <thead>
    <tr>
        <th scope="col">Username</th>
        {{#showDelete}}
            <th scope="col">Delete</th>
        {{/showDelete}}
    </tr>
    </thead>
    <tbody>
    {{#users}}
        <tr>
            <td>{{login}}</td>
            {{#showDelete}}
                <td>
                    <a href="/admin/users/delete/{{id}}">DELETE</a>
                </td>
            {{/showDelete}}
        </tr>
    {{/users}}
    </tbody>
</table>

{{>footer}}